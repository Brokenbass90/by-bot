# Аудит бота + точные правки для Codex — 2026-06-08

*Автор: Opus (аудит-сессия). Один документ для Codex. Правки в порядке приоритета.*
*Принцип: ничего не ломать. Деплой — только Codex после `open_trades=0`.*

---

## 🎯 ПЛАН ДЛЯ CODEX ПО ПРИОРИТЕТУ (читать первым)

Порядок важен: пункты 1-2 — это «почему бот не зарабатывает», остальное — после.

1. **P0 — деплой фикса стопов** (уже в коде, см. changelog ниже). Ревью diff → `tests/` → деплой на `open_trades=0` → сверить `request_sl ≈ sl_price` на первых сделках.
2. **P0-b — проверить `NO_ENTRY_HOURS_UTC` на сервере** (`grep NO_ENTRY_HOURS_UTC /root/by-bot/.env`). Если режет большую часть суток — вернуть к `0,1,2`. Это разблокирует частоту сделок.
3. **После 1-2: 2-3 недели наблюдения** live vs backtest на ядре (att1, breakdown, midterm, flat). Свериться, появился ли реальный эдж.
4. **Триаж сложности** (параллельно, безопасно): запустить `scripts/archive_dead_strategies.py --apply` (9 реально безопасных файлов; остальные 33 «мёртвых по импорту» связаны через реестр/конфиги — НЕ трогать автоматом). 262 sweep-конфига проредить вручную. Core свести к 4-6 стратегиям.
5. **ATT1 v2:** бэктест v2 против v1 → если выигрывает, сделать `att1_v2_live.py` и переключить.
6. **Спокойные рукава** (после того как ядро крипты зарабатывает): funding-carry (валидировать ≥10 циклов) → Alpaca paper→real gate.

Детали по каждому пункту — в разделах ниже.

---

## ✅ Сделано в этой сессии (changelog) — для Codex

**P0-фикс уже подготовлен в коде (готов к ревью и деплою).** Живой сервер не тронут — правка только в репозитории.

Изменённые файлы:
1. `smart_pump_reversal_bot.py`
   - добавлена константа `LEGACY_PCT_STRATEGIES = {"pump", "pump_fade"}` (после `SL_PCT`, ~строка 937);
   - в `sync_trades_with_exchange()` (~строка 6137) логика выставления TP/SL при филле переписана: теперь по умолчанию **сохраняются уровни стратегии**, а глобальный pct-фолбэк (0.5%/0.3%) применяется только к legacy pump-стратегиям или если стратегия не задала уровни;
   - импорт обновлён: добавлен `should_preserve_strategy_tpsl`.
2. `bot/tpsl_policy.py` — добавлена чистая функция `should_preserve_strategy_tpsl(...)` (вся логика решения в одном месте, тестируема).
3. `tests/test_tpsl_policy.py` — добавлены 4 регресс-теста (всего 8, все зелёные).

Проверки: `py_compile` OK для обоих файлов; все 8 тестов `tests/test_tpsl_policy.py` проходят.
4. `strategies/att1_v2_live.py` — НОВЫЙ ready-to-test wrapper под ATT1 v2 (smoke-тест пройден: импорт+вызов OK). Не подключён; для бэктеста Codex'ом, см. раздел ATT1.

**Codex, при возвращении:**
- [ ] просмотреть `git diff` (2 файла кода + 1 тест);
- [ ] прогнать `tests/` целиком в нормальном окружении (в песочнице pytest спотыкается на сканировании `backtest_runs/` — это не ошибка кода);
- [ ] задеплоить на `open_trades=0`;
- [ ] на первых 3-5 сделках сверить в `runtime/live_mirror/live_trade_events.jsonl`, что `request_sl` ≈ `sl_price` (до фикса расходились ~10×).

---

---

## TL;DR за 60 секунд

Бот **живой и торгует реальные деньги** (`dry_run=false`, аптайм ~3.5 дня, режим `bear_chop`). Это не «заморожен», как считал хэндофф 29 мая.

**Найдена одна критическая ошибка (P0), которая тихо убивает почти весь портфель:** при заполнении ордера бот **выбрасывает спроектированный стратегией стоп-лосс и подставляет жёсткие 0.3%**. Из-за этого каждая сделка ловит рыночный шум и закрывается по стопу за 1-3 минуты. Это объясняет: (1) почему бот «плохо торгует», (2) почему live расходится с бэктестом, (3) серию мгновенных стопов 5-6 июня.

**Главный вывод:** сначала чиним механику исполнения (P0), и только потом думаем про новые стратегии и арбитраж. Любая стратегия поверх сломанного исполнения будет течь. Цифрам бэктеста (+73.96% / PF 1.591) **нельзя верить как прогнозу live, пока P0 не исправлен** — бэктест использует настоящие стопы, live использует 0.3%.

---

## P0 — КРИТИЧНО: стоп-лосс стратегии затирается на 0.3% при филле

### Где
`smart_pump_reversal_bot.py`, строки **6134-6146** (внутри sync-цикла, переход `PENDING_ENTRY → OPEN`).

```python
if getattr(tr, "strategy", "pump") in ("bounce", "range", "inplay", "inplay_breakout", "btc_eth_midterm_pullback"):
    # сохраняем стоп стратегии (re-round абсолютных уровней)
    tr.tp_price, tr.sl_price = round_tp_sl_prices(sym, tr.side, tr.avg, tr.tp_price, tr.sl_price)
else:
    # ИНАЧЕ — затираем глобальными TP_PCT=0.5% / SL_PCT=0.3%
    ...
    tr.tp_price, tr.sl_price = round_tp_sl_prices(sym, tr.side, tr.avg, tp_raw, sl_raw)
```

Глобальные дефолты: `TP_PCT = 0.50`, `SL_PCT = 0.30` (строки 935-936).

### Что происходит
Белый список «сохранить стоп стратегии» содержит всего 5 имён: `bounce, range, inplay, inplay_breakout, btc_eth_midterm_pullback`. Этот список написан в эпоху, когда стратегий было мало, и **никто не обновлял его по мере добавления 80+ стратегий**. Все современные стратегии в него не попадают и при филле получают жёсткий стоп 0.3% / тейк 0.5%.

### Радиус поражения (имена из `tr.strategy =`)
**Сейчас торгуют в live и ВСЕ поражены** (стоп → 0.3%):
- `att1_trendline_touch` (ATT1)
- `flat_resistance_fade` (ARF1)
- `alt_inplay_breakdown_v1` (Breakdown) ← подтверждённый блед 5-6 июня
- `impulse_volume_breakout_v1` (IVB1)
- `hzbo1_zone_break` (HZBO1)

Также поражены (если включат): `sloped_channel`, `asb1_slope_break`, `asm1_sloped_momentum`, `elder_triple_screen_v2`, `alt_bear_regime_continuation_v1`, `alt_support_bounce_v1`, `alt_support_reclaim_v1`, `micro_scalper_v1`, `retest_levels`, `session_open_breakout_v1`, `triple_screen_v132`.

**Защищён только `btc_eth_midterm_pullback` (midterm).**

### Доказательство из live-журнала (`runtime/live_mirror/live_trade_events.jsonl`)
ONDOUSDT short, 6 июня:
- `order_submitted`: `request_sl = 0.3278` (≈4% = настоящий стоп стратегии 1.8×ATR)
- `entry_filled`: `sl_price = 0.3185` (≈0.3% = глобальный SL_PCT)
- `close`: SL hit через 51 секунду, pnl −$0.13

Тот же паттерн на ETHUSDT (−$0.13, 95 сек), ADAUSDT (−$0.25, 50 сек). Подряд.

### Почему это двойной урон
1. **Винрейт убит:** стоп 0.3% на активе с ATR ~2.4% — это чистый шум. Цена задевает стоп почти всегда.
2. **Risk/Reward убит:** тейк 0.5% вместо структурной цели стратегии (часто 4-8%). Даже когда стратегия права, прибыль микроскопическая.
3. Позиция при этом **рассчитана под широкий стоп** (sizing берёт `stop_pct` из настоящего стопа стратегии, строка ~10895), поэтому каждый убыток мелкий — но частый. Классический блед + комиссии.

### Правка (для Codex) — инвертировать логику: сохранять стоп для ВСЕХ, кроме явного legacy-набора
Заменить условие на строке 6134 так, чтобы pct-пересчёт применялся **только** к настоящим pump-стратегиям, а у всех остальных стоп/тейк стратегии сохранялся. И обязательно фолбэк, если стратегия не дала sl/tp.

```python
LEGACY_PCT_STRATEGIES = {"pump", "pump_fade"}  # вынести в константу наверх

strat_name = str(getattr(tr, "strategy", "pump") or "pump")
has_strategy_levels = (tr.tp_price is not None) and (tr.sl_price is not None)

if strat_name not in LEGACY_PCT_STRATEGIES and has_strategy_levels:
    # современная стратегия: сохраняем её абсолютные уровни, только re-round от avg
    tr.tp_price, tr.sl_price = round_tp_sl_prices(sym, tr.side, tr.avg, tr.tp_price, tr.sl_price)
else:
    # legacy pump ИЛИ стратегия не задала уровни → глобальный pct-фолбэк
    if tr.side == "Sell":
        tp_raw = tr.avg * (1.0 - TP_PCT / 100.0); sl_raw = tr.avg * (1.0 + SL_PCT / 100.0)
    else:
        tp_raw = tr.avg * (1.0 + TP_PCT / 100.0); sl_raw = tr.avg * (1.0 - SL_PCT / 100.0)
    tr.tp_price, tr.sl_price = round_tp_sl_prices(sym, tr.side, tr.avg, tp_raw, sl_raw)
```

### Проверка перед деплоем (обязательно)
1. Прогнать существующие unit-тесты (`tests/`), убедиться что midterm/bounce поведение не изменилось.
2. После деплоя — на первых 3-5 сделках сверить `request_sl` ≈ `sl_price` в `live_trade_events.jsonl` (раньше расходились в ~10x).
3. Включать на `open_trades=0`.

### Ожидаемый эффект (честно, без обещаний)
Это **не «добавит +X%»**, а **уберёт структурный слив**: стратегии начнут работать так, как они спроектированы и как считались в бэктесте. Это необходимое условие, чтобы вообще проверить, есть ли у стратегий реальный эдж. До этой правки любые цифры live бессмысленны.

---

## P1 — Важное (после P0)

### 1. Перепроверить бэктесты после P0
Все бэктест-цифры (включая baseline +73.96% / PF 1.591) считались с настоящими стопами, а live торговал с 0.3%. После фикса P0 — прогнать заново 1-2 ключевые стратегии (ATT1, breakdown) на свежих 90 днях и сравнить live vs backtest. Только так поймём настоящий эдж.

### 2. Риск-рейлы есть — проверить, что они реально срабатывают
Хорошая новость: каркас риска построен грамотно.
- Sizing от риска: `risk_usd = equity × RISK_PER_TRADE_PCT`, `notional = risk_usd / stop%` (строка 7365). Корректно.
- Дневной лимит потерь `DAILY_LOSS_LIMIT_PCT = 2.0` и аварийный стоп `MAX_DRAWDOWN_PCT = 5.0` (строки 1219-1220), проверяются в `portfolio_can_open` (строки 8206-8211).

**Задача Codex:** подтвердить тестом, что при просадке дня >2% и общей >5% бот реально перестаёт открывать сделки (не только пишет в лог). Это страховка психики владельца.

### 3. Латентность фикс-детекции
`latency_send_to_fill_sec = 26-36с` в журнале — это **лаг поллинга** sync-цикла, а не реальная задержка (входы — Market/IOC, исполняются мгновенно). Само по себе не критично, но: для тайтовых сетапов фикс детектится поздно. После P0 стопы широкие — проблема уходит. Низкий приоритет, просто не пугаться этих цифр.

### 4. `MAX_POSITIONS` дефолт = 1 (строка 1218)
В коде хардкод-дефолт 1, хэндофф упоминал 3. Подтвердить, какое значение реально в live `.env`, и осознанно зафиксировать (при риске 1-2% на сделку и капитале $123 — 1-2 позиции разумно).

---

## Состояние библиотеки стратегий (честно)

87 файлов стратегий, 240+ sweep-конфигов, ~14-20 wired, реально протестированных единицы. **Главный системный риск — не нехватка идей, а переоптимизация и сложность.** 240 sweep-прогонов по одной выборке почти гарантируют, что часть «победителей» — подгонка под прошлое (overfit), а не эдж.

Рекомендация: после P0 **сузить live до 3-4 стратегий с реальным, повторяемым эджем** (кандидаты: midterm BTC/ETH — уже защищён; ATT1 и breakdown — после фикса проверить заново). Остальное — в shadow/paper, не в live. Меньше стратегий → проще контролировать → меньше точек отказа.

---

## Арбитраж и «спокойный рукав» (честный статус)

Инфраструктуры много (`scripts/funding_carry_executor.py`, `cross_exchange_funding_shadow.py`, basis_arb), ключи Binance/Bitget подключены read-only. **Но реальной прибыли пока ноль:** `runtime/arb_roi_estimate.json` → `status: insufficient_closed_cycles, closed_cycles: 0`. Арбитраж **не подключён к live-боту** и работает только в shadow/dry-run.

Это нормально для стадии, но важно для ожиданий: **спокойный funding-рукав ещё не зарабатывает и не валидирован.** Прежде чем заводить туда живые деньги — нужно ≥10 закрытых shadow-циклов с положительным ROI после комиссий и расходов на фандинг.

---

## Распределение капитала $2500 — предложение под профиль риска 1-2%/сделку

Ориентир, не приказ. Идея — не вводить всё сразу, а по мере валидации.

| Этап | Капитал | Куда | Условие входа |
|------|---------|------|----------------|
| Сейчас | $123 (есть) | Bybit, агрессивный рукав | После P0-фикса — наблюдаем 2-3 недели |
| Шаг 1 | +$300-400 | Bybit агрессивный | Если после P0 live ≈ бэктест и DD < 5% за 2-3 нед |
| Шаг 2 | $300-500 | Funding/basis arb (спокойный) | Только после ≥10 прибыльных shadow-циклов |
| Резерв | оставшиеся | Не вводить | Доливать по факту работающего трека |

**Логика:** агрессивный рукав (directional стратегии) — это «максимальный заработок», но он же источник просадок; его масштабируем только после доказанного эджа на живых деньгах. Спокойный рукав (funding carry) — это «медленные, но стабильные» деньги, но он ещё не доказан. Не заводить $2500 разом ни в один из них.

---

## Что НЕ делать

- ❌ Не наращивать капитал и не добавлять стратегии в live, пока P0 не исправлен и не подтверждён на живых сделках.
- ❌ Не верить бэктест-цифрам как прогнозу live до фикса P0.
- ❌ Не заводить живые деньги в арбитраж без ≥10 прибыльных закрытых shadow-циклов.
- ❌ Не повышать leverage > 3x.
- ❌ Не включать новые стратегии в live пачкой — по одной, с наблюдением.

---

## Порядок действий для Codex (чек-лист)

1. [ ] P0: правка строк 6134-6146 (инверсия whitelist + фолбэк). Прогнать `tests/`.
2. [ ] Деплой P0 на `open_trades=0`. Сверить request_sl ≈ sl_price на первых сделках.
3. [ ] Подтвердить срабатывание дневного лимита (2%) и max DD (5%) тестом.
4. [ ] Через 2-3 недели: live vs backtest по ATT1/breakdown на свежих данных.
5. [ ] Решить состав live = 3-4 стратегии с реальным эджем, остальное в shadow.
6. [ ] Арбитраж держать в shadow до ≥10 прибыльных циклов.

---

## Арбитраж — углублённая оценка (2026-06-08)

Инфраструктура построена честно: `cross_exchange_funding_scan.py` фильтрует по объёму (мин $1M), `cross_exchange_funding_validate.py` проходит по реальному стакану и считает net после taker-комиссий и проскальзывания, `arb_roi_calculator.py` принципиально отказывается прогнозировать ROI до ≥10 закрытых циклов (модель `settlement_execution_v2`). Это правильно.

Две проблемы:
1. **Порог `--min-spread-apr-pct` = 36%** → система видит только экстремальные спреды на свежих микро-капах (BEAT, ESPORTS, «APR 350%+»). Они реальны, но некапчабельны: тонкая ликвидность, разворот фандинга, squeeze шорт-ноги, плохое проскальзывание на выходе. Ликвидные пары (BTC/ETH/SOL, спред единицы-десятки % годовых) отсекаются этим порогом.
2. **0 закрытых циклов** — dry-run блокируется балансом (`insufficient_balance`, binance $11.70 / bitget $50). Даже бумажный режим не открывает циклы → нет данных для честного ROI.

**Вывод:** для счёта $2500 арбитраж — тонкий и рискованный «спокойный» рукав, не приоритет. Главные деньги — направленная крипта после деплоя P0.

**Что сделать с арбитражем (низкий приоритет, без живых денег):**
- [ ] Добавить второй профиль сканера на ликвидные мажоры (низкий `min-spread-apr`, высокий `min-volume`), чтобы оценить реальный безопасный (пусть малый) эдж.
- [ ] Разрешить dry-run открывать бумажные циклы по нотиональной модели независимо от живого баланса → накопить ≥10 закрытых циклов и получить честный net-ROI за ~неделю наблюдения.
- [ ] Решение о живых деньгах в арбитраж — только после положительного net-ROI на ≥10 циклах.

## Бэктест — проверка честности (2026-06-08): ✅ движок надёжен

`backtest/engine.py`:
- комиссия `fee_bps=6.0` (0.06%/сторона) и проскальзывание `slippage_bps=2.0` учитываются на входе и выходе;
- **same-bar SL+TP → берётся SL первым** (консервативно, без оптимистичного перекоса) — строка ~458;
- управление позицией только с бара ПОСЛЕ входа (`i > pos.entry_i`) — нет look-ahead;
- sizing по дистанции стопа — как в live.

**Вывод:** методология бэктеста заслуживает доверия. Расхождение live vs backtest объясняется (1) багом P0 — исправлен, и (2) риском переоптимизации (240+ sweep). После деплоя P0 live должен сойтись к бэктесту. Против overfit — дисциплина OOS-тестов (раз в квартал прогон на данных, не участвовавших в sweep), а не правка кода.

---

## P0-b — КРИТИЧНО: бот почти не входит из-за фильтра по времени (NO_ENTRY_HOURS)

### Симптом (из live heartbeat, runtime/live_mirror/bot_heartbeat.json)
За ~3.5 дня: ATT1 — 18210 проверок → 319 try → **2 входа**; breakdown — 36020 → 8 входов. Доминирующая причина отказа в `portfolio_can_open`: **`no_entry_hour` = 16969 из 17061** портфельных отказов ATT1 (≈99.5%).

### Причина
`smart_pump_reversal_bot.py:8191` — гейт `NO_ENTRY_HOURS_UTC`. Дефолт = «0,1,2» (3 часа). Но в live он блокирует ~весь день → на сервере `NO_ENTRY_HOURS_UTC` переопределён на широкое окно (серверный `.env` я из песочницы не вижу).

### Эффект
Даже после фикса P0 бот не заработает, если он почти не открывает сделки. Это второй тормоз уровня P0.

### Задача Codex (проверить на сервере)
- [ ] `grep NO_ENTRY_HOURS_UTC /root/by-bot/.env` — посмотреть реальное значение.
- [ ] Если блокирует большую часть суток без явной причины — вернуть к `0,1,2` (или к осмысленному окну тонкой ликвидности 1-3 часа).
- [ ] Параллельно проверить, не режет ли частоту входов ещё и `global_risk_mult` (в bear_chop = 0.55) через sizing → notional < MIN_NOTIONAL → пропуск. Свериться по `*_skip_notional_small`.

**Ожидаемый эффект:** восстановление нормальной частоты сделок. Вместе с P0 это и есть «дать боту реально торговать».

## P1 — Риск-стопы: ✅ проверено, реально срабатывают

`portfolio_can_open` (8159-8222): при `cur_eq < start*(1-5%)` (max DD) и `< day_start*(1-2%)` (дневной лимит) ставит `PORTFOLIO_STATE["disabled"]=True` и блокирует ВСЕ новые входы (флаг проверяется первым, строка 8188). Это не просто лог — торговля реально останавливается. Психологическая страховка работает. (Caveat: `disabled` держится до сброса; `day_equity_start` сбрасывается раз в сутки — это корректно.)

---

## Чек-лист безопасности (расписать → реализовать)

Статус сейчас: реальные ключи только Bybit (в `.env` на сервере, 600, не в git); Binance/Bitget — read-only тестовые; авто-вывода в коде нет. Базово ОК.

Реализовать по мере роста капитала:
1. **Раздельные API-ключи по функциям:** read-only (данные) / trade-only без вывода (торговля) / withdraw-only (только модуль переноса). Сейчас торговый ключ не должен иметь права вывода.
2. **Whitelist адресов вывода** на каждой бирже — главная защита от слива при утечке ключа.
3. **IP-allowlist** на ключах = статичный IP сервера (ещё один аргумент за VPS, не дом).
4. **Веб-интерфейс замены ключей** (план пользователя): хранить ключи только на сервере, в офлайне не держать. До этого — копия `.env` есть и локально, помнить про это.
5. **Авто-перенос между биржами — НЕ делать на старте.** Когда дойдём: лимиты + ручное подтверждение в Telegram для сумм выше порога; шифрование withdraw-ключа вне репозитория.
6. **AI-советник по ребалансу** (идея пользователя): сначала режим «советует, человек подтверждает», полная автоматика — сильно позже.

---

## Решение по арбитражу (2026-06-08)

Ничего не отключать — Binance/Bitget read-only shadow безвреден и бесплатно копит данные. Основная связка — **Bitget↔Bybit** (bitget $50 > binance $11.70). Живых денег и сил сейчас не вкладывать. Вернуться, когда заработает направленный рукав.

---

## ATT1 — разбор (2026-06-08): улучшенная версия написана, но не подключена

### Находка
Live ATT1 использует **v1** (`strategies/att1_live.py` → `AltTrendlineTouchV1Strategy`). При этом существует **v2** (`strategies/alt_trendline_touch_v2.py`, 902 строки, от 25 мая) — «Улучшенная версия ATT1»: взвешенная линейная регрессия с decay, R²-порог качества линии, scoring сетапа (`_signal_quality`), volume-confirm, раздельные RSI-гейты. v2 **нигде не импортируется** — живой обёртки под неё нет.

### Системный паттерн (важно)
Это не единичный случай. По проекту улучшенные версии и новые стратегии пишутся, но не включаются: MTPB (19 sweeps, не подключена), несколько «готовых» стратегий ждут включения. **Колоссальный объём написанного кода не работает в live.** Это и есть та самая сложность, которая мешает: усилия уходят в код, который не торгует.

### Почему ATT1 мало входит (live: 319 проверок → 2 входа)
Помимо гейта по времени (P0-b), ATT1 низкочастотна по конструкции: решение раз в ~55 мин (`ATT1_DECISION_MIN_AGE_MIN=55`), кулдаун 8 ч на символ (`cooldown_bars_5m=96`), и строгий фит трендлайна — доминирующая причина no-signal `ns_trendline=126` (мало пивотов / низкий R² / наклон вне диапазона). Это нормальная селективность, но на маленьком универсуме даёт почти ноль сделок.

### Что делать (для Codex, всё через бэктест — НЕ деплоить вслепую)
1. [ ] **Бэктест v2 против v1** на одном окне (последние 90-180 дней, тот же универсум, те же fee/slippage). Метрики: PF, DD, число сделок, winrate. Acceptance: v2 проходит, если PF не хуже v1 при ≥ числе сделок и DD ≤ baseline.
2. [ ] Если v2 выигрывает — сделать живую обёртку `att1_v2_live.py` (по аналогии с `att1_live.py`, импорт `alt_trendline_touch_v2`) и переключить ATT1_ENGINE. (Могу подготовить этот wrapper заранее по запросу — это новый файл, live не трогает.)
3. [ ] Отдельно прогнать sweep кулдауна (96→48 bars) и `min_pivots`/R²-порога: даёт ли больше сделок без падения PF.
4. [ ] Ревизия «написано, но не подключено»: составить список таких стратегий и для каждой решить — бэктест+wire или в архив. Сложность сократить.

**Вывод по приоритету:** для ATT1 главный рычаг частоты — это P0-b (гейт по времени), а главный рычаг качества — возможный переход на v2 (после бэктеста). Слепая правка параметров запрещена (overfit).

---

## Карта стратегий и триаж сложности (2026-06-08)

### Цифры
- 83 файла в `strategies/`.
- **41 не импортируются нигде в активном коде → мёртвый груз** (см. список ниже).
- ~20 семейств подключены к боту (`try_*_entry`), но live-включены единицы; из-за P0 корректно торговала по сути только midterm.
- **262 конфига** в `configs/autoresearch/` — разросшийся ворох sweep'ов.
- Улучшенные/ресёрч-версии подключены только к скриптам, не к боту: `alt_trendline_touch_v2`, `basis_arb_v1`, `funding_hold_v1`, `funding_rate_reversion_v1` и серия session-стратегий.

### Подключено к live-боту (20 семейств, `try_*_entry`)
asb1, asm1, att1, bounce1, brc1, breakdown, breakout, elder, flat, hzbo1, inplay, ivb1, micro_scalper, midterm, range, retest, sloped, sob1, support_reclaim, ts132.
**Реально активны по heartbeat:** att1, breakdown, flat, midterm, ivb1 (+ периодически hzbo1/sloped). Остальные включаются оверлеем режима.

### Мёртвый груз — 41 файл (импортируются нигде; кандидаты в `archive/`)
alt_bear_breakdown_v1, alt_bear_consolidation_short_v1, alt_elder_revived_v1, alt_inplay_breakdown_v2, alt_liquidity_sweep_reversal_v2, alt_momentum_breakout_v1, alt_pullback_continuation_v1, alt_range_reclaim_v1, alt_range_scalp_v1, alt_spike_rejection_v1, alt_squeeze_breakout_v1, alt_stablecoin_depeg_arb_v1, alt_volume_spike_momentum_v1, alt_vwap_mean_reversion_v1, alt_whale_print_follow_v1, btc_cycle_continuation_v1, btc_cycle_level_target_v2, btc_cycle_pullback_v1, btc_daily_level_reclaim_v1, btc_macro_cycle_v1, btc_regime_flip_continuation_v1, btc_regime_retest_v1, btc_sloped_reclaim_v1, btc_swing_zone_reclaim_v1, btc_weekly_zone_reclaim_v2, elder_crypto_v1, elder_triple_screen_v3, funding_rate_reversion_v1, liquidation_cascade_entry_v1, micro_scalper_bounce_v1, micro_scalper_breakout_v1, pump_fade_simple, pump_fade_v2, pump_fade_v3, pump_fade_v4r, pump_momentum_v1, sc1_live, scalper_bounce_v2, scalper_breakout_v2, scalper_sweep_v2, sloped_break_retest_v1, sloped_resistance_choch_v1.

*(Caveat: пара файлов может грузиться динамически — Codex проверяет перед перемещением.)*

### Триаж — план (для Codex, через бэктест)
1. [ ] **Сократить core до 4-6 стратегий** с доказанным эджем. Кандидаты ядра: midterm (защищён, работал), att1 (после v2-бэктеста), breakdown, flat/arf1. Остальные live-семейства — в shadow до подтверждения.
2. [ ] **41 мёртвый файл → `archive/strategies_retired/`** (не удалять, переместить). Сразу минус огромный кусок сложности.
3. [ ] **262 sweep-конфига → проредить.** Оставить активную очередь (5-7 пакетов), остальное в `configs/autoresearch/_archive/`. 262 конфига = почти гарантированный overfit-риск и шум.
4. [ ] Для нескольких *перспективных но непротестированных* идей (liquidation_cascade, whale_print_follow, vwap_mean_reversion, squeeze_breakout) — по одному бэктесту, прежде чем решать wire/archive.

**Принцип:** меньше стратегий, которые понятны и протестированы > много, которые не работают. Сложность — это враг №1 для маленькой команды.

### «Что полезного для каждого рынка» — честно, кратко
Сейчас добавлять новое преждевременно: сначала ядро должно зарабатывать. Но семена на потом:
- **Крипта:** не новая directional-стратегия, а funding-carry как спокойный рукав (после валидации) + дисциплина OOS.
- **Акции (Alpaca):** уже есть momentum + sector rotation — это и есть правильный спокойный рукав; доделать paper→real gate.
- **Арбитраж:** профиль на ликвидные мажоры (см. раздел арбитража), а не микро-капы.
Полноценную проработку по рынкам — Alpaca → арбитраж → ядро/AI/TG/веб — делаем после того, как ядро крипты начнёт зарабатывать.


---

## УТОЧНЕНИЕ по триажу сложности (важно, исправление)

Первичная оценка «41 мёртвый файл» оказалась неточной. Проверка ссылок **по имени-строке** (а не только по `import`) показала: бот wire-ит многие стратегии **динамически через реестр и конфиги** — `scripts/build_strategy_registry.py`, `configs/portfolio_allocator_policy*.json`, `configs/strategy_health_*.json`, canary-env, sweep-конфиги. Пример: `alt_range_scalp_v1` ссылается прямо в `smart_pump_reversal_bot.py` и `bot/health_gate.py` — он НЕ мёртвый.

**Итог проверки 41 файла:**
- **9 реально безопасны к архивации** (ноль ссылок где-либо): `alt_bear_breakdown_v1`, `alt_bear_consolidation_short_v1`, `alt_stablecoin_depeg_arb_v1`, `btc_daily_level_reclaim_v1`, `btc_macro_cycle_v1`, `btc_swing_zone_reclaim_v1`, `btc_weekly_zone_reclaim_v2`, `pump_fade_v3`, `sc1_live`.
- **33 связаны через реестр/конфиги** — массово удалять нельзя, сначала вычищать ссылки.

**Готов скрипт:** `scripts/archive_dead_strategies.py` — перемещает только эти 9 в `archive/strategies_retired/`, dry-run по умолчанию, с перепроверкой ссылок в рантайме перед каждым перемещением (`--apply` для выполнения). Прогон dry-run: 9 to move, 0 skipped. После `--apply`: `py_compile smart_pump_reversal_bot.py` + `tests/`.

**Архитектурный вывод:** сокращение сложности — это не «удалить файлы», а «вычистить ссылки в реестре/allocator-политиках/health-гейтах, потом удалить файл». Это аккуратная работа на стороне конфигов, и её должен делать Codex с бэктест-проверкой, что портфель не сломался.

---

## breakdown (alt_inplay_breakdown_v1) — разбор (2026-06-08)

Дизайн разумен и второго бага нет. Short-only continuation/failed-reclaim: gated на медвежий режим (`regime_not_bearish`), требует реального пробоя 1h-поддержки (`no_real_break`/`break_too_small`) с импульсной свечой (`min_break_body_frac=0.35`), RSI-фильтр, ATR-стоп 1.8×, next-level TP, time-stop 24ч. Доминирующий `structure_idle` в heartbeat — это **нормально**: пробои редки, большую часть времени сетапа просто нет.

**Вывод:** breakdown губил исключительно P0 (стоп → 0.3%). После фикса P0 — обязателен бэктест на свежих 90-180 днях, чтобы увидеть реальный эдж. Параметры (cooldown, min_break_atr, RSI) трогать только через sweep, не вслепую. Отдельных правок кода не требуется.

---

## Веб-интерфейс — ревью удобства (2026-06-08)

`web/static/index.html` (3297 строк, React 18 через CDN, `React.createElement` без JSX). **Сделан компетентно**, не сырой: 14 страниц, поллинг 15с на ключевых, пагинация, графики (Chart.js + lightweight-charts), TOTP-авторизация, адаптивные grid-media-queries, обработка loading/error (~87 мест). Страница `API Keys` (ротация + cross-exchange ключи) **уже есть**.

Это не переписывание, а точечные усиления. Приоритет — под профиль владельца (чувствителен к просадкам → нужен спокойный, ясный статус).

### Топ-улучшения (по ценности)
1. **Статус-полоса в шапке Dashboard** (🔴 главное): equity, P&L дня, открытые позиции, режим, risk-mult, `trade_on/paused`, и **запас до лимитов** (сколько % осталось до дневного −2% и общего −5%). Прямо снимает тревогу: видно, далеко ли до аварийного стопа.
2. **Индикатор «бот жив»** (🔴): возраст последнего heartbeat (`runtime/live_mirror/bot_heartbeat.json` ts). Раньше бот «казался замороженным» — ясный «alive 30s ago / ⚠ stale 2h» убирает эту неопределённость.
3. **Лента живых событий** (🟠): последние entry/exit/SL из `live_trade_events.jsonl` прямо в UI (сейчас видно только в TG). + после деплоя P0 мини-панель «request_sl ≈ sl_price» для контроля фикса.
4. **Мобильная навигация** (🟠): сайдбар фиксирован 220px; на телефоне нужен сворачиваемый «гамбургер» — владелец смотрит на ходу.
5. **Freshness на каждой странице** (🟡): «данные обновлены Xс назад» + единый авто-рефреш (сейчас поллинг только на части страниц).
6. **Pause/Resume и risk-mult прямо в UI** (🟡): кнопки управления (есть в TG `/pause /resume /risk`) продублировать в Dashboard с подтверждением.

### Принцип
Не трогать рабочий 3297-строчный файл вслепую — изменения UI нужно проверять глазами. Реализуем точечно: либо я делаю аддитивные правки → ты смотришь визуально → Codex деплоит; либо сперва соберу мокап нужного Dashboard, чтобы согласовать вид.

---

## РОАДМАП РАБОТ (живой список — чтобы не потерять планы)

### Сделано в этой сессии (2026-06-08)
- P0 (стопы) — фикс в коде + тесты. P0-b (NO_ENTRY_HOURS) — найдено, ждёт проверки Codex.
- Обёртка `att1_v2_live.py`, скрипт `archive_dead_strategies.py`, карта стратегий, ревью арбитража/бэктеста/веба, мокап Dashboard.

### Бэктест-дисциплина (СДЕЛАНО)
- [x] **`backtest/robustness.py`** + `tests/test_robustness.py` (12/12 зелёные): `jitter_rows` (±0.2% шум, сохраняет инварианты свечей), `walk_forward_windows` (rolling IS→OOS), `aggregate_oos` (вердикт robust/fragile по медиане OOS). Проверено на реальных данных. **Применять ко всем sweep'ам.**

### Новый рукав — pair trading (СДЕЛАНО, ждёт бэктеста)
- [x] **`strategies/pair_stat_arb_v1.py`** + `tests/test_pair_stat_arb.py` (9/9 зелёные): market-neutral ETH/BTC. Hedge-ratio (OLS), z-score спреда, half-life как прокси коинтеграции, корректное распределение ног по знаку z, exit/stop логика. Чистый stdlib. На реальных ETH/BTC: beta≈1.01, corr≈0.90, half-life≈4.6 бара — осмысленно. **Codex: бэктест с walk-forward+jitter+комиссии → если net OOS PF>1, написать executor двух ног.**

### Бэктест-дисциплина (старый пункт — заменён выше)
- [~] **Jittering** (±0.2% шум к ценам) + **walk-forward** (rolling in-sample → out-of-sample) — модуль `backtest/robustness.py` + тесты. Честная метрика = среднее по OOS, не in-sample. Применять ко всем будущим sweep'ам (анти-overfit).

### Новые рукава (по очереди, всё через бэктест)
- [ ] **Pair trading ETH–BTC** (market-neutral statarb): z-score спреда, коинтеграция, риск 0.7% на пару. Только OHLCV (есть). Спокойный рукав, нейтрален к направлению.
- [ ] Funding-carry — валидировать ≥10 циклов, потом живые деньги.

### Веб — усиления (после ядра, делаем красиво)
- [ ] Статус-полоса + «бот жив» + запас до стоп-лимитов (мокап готов).
- [ ] **Больше контроля сделок:** мониторинг открытых позиций с биржи + график + аналитика по позиции; кнопки «закрыть сделку», «долить депозит/увеличить позицию» — больше гибкости прямо из UI.
- [ ] **Кликабельный P&L дня вверху** → модалка: P&L по дням и месяцам, разбивка **по рукавам** (сколько заработал каждый), + **налоговый калькулятор под Кипр** (сколько отложить на налоги).
- [ ] Лента событий в UI, мобильная навигация, freshness-индикаторы, pause/risk-mult кнопки.
- [ ] Общая эстетика: чисто, аккуратно, приятно глазу.

### Переосмыслить (красные флаги DeepSeek — не выбрасываем, делаем лучше)
- [ ] Order Flow / поглощение — нужен сбор L2-стакана (инфраструктура данных). Спроектировать сбор тиков отдельно.
- [ ] VPVR / Volume Profile — реалистично, кандидат на уровни входа/выхода.
- [ ] ML / RL — позже и проще (не микросервис): начать с лёгкого фильтра качества сигналов, не с LSTM/PPO.

### Большая последовательность
ядро крипты зарабатывает → расширение и доводка стратегий → доводка вспомогательных систем (TG-бот, DeepSeek-слой, веб) → Alpaca paper→real → арбитраж. **После этого** — Claude предлагает и внедряет новые идеи.

---

## 📦 ДЛЯ CODEX: что слать и что в этом репо изменено (2026-06-08)

**Этот документ (`AUDIT_AND_FIXES_2026_06_08.md`) — главный для Codex.** (Не путать с `docs/SESSION_LOG_2026_06_03.md` — это старый лог от 3 июня.)

### Изменённые файлы (deploy-критичные)
- `smart_pump_reversal_bot.py` — P0-фикс (TP/SL при филле) + импорт.
- `bot/tpsl_policy.py` — `should_preserve_strategy_tpsl()`.
- `tests/test_tpsl_policy.py` — +4 регресс-теста (8/8).

### Новые файлы (не подключены к live; для бэктеста/работы Codex)
- `strategies/att1_v2_live.py` — обёртка ATT1 v2 (smoke OK).
- `strategies/pair_stat_arb_v1.py` + `tests/test_pair_stat_arb.py` (9/9) — pair trading ETH/BTC.
- `backtest/robustness.py` + `tests/test_robustness.py` (12/12) — jitter + walk-forward.
- `scripts/archive_dead_strategies.py` — безопасная архивация 9 файлов (dry-run).
- `scripts/pnl_by_sleeve.py` — P&L по рукавам/дням/месяцам из журнала (фундамент P&L-модалки).

### Проверка деплоя по приоритету: см. раздел «🎯 ПЛАН ДЛЯ CODEX» вверху.

### Реальное подтверждение бледа (вывод `pnl_by_sleeve.py` на 2026-06-08)
Последние 19 закрытых сделок: total −$3.09 (fees $0.58), W/L 5/14. Худший: `alt_inplay_breakdown_v1` −$2.42 (4W/11L) — ровно жертва P0. После деплоя P0 этот же отчёт должен показать иную картину (стопы перестанут резать на шуме).

## ▶ ВОЗОБНОВИТЬ В СЛЕДУЮЩЕЙ СЕССИИ (для будущего Claude — обновлено 2026-06-08 late)

**Статус:** аудит завершён; ядро (P0) починено в коде; 8 новых файлов (модули/скрипты/тесты), 41/41 тестов зелёные; всё ждёт деплоя Codex (вернётся 11-го). Живой сервер не тронут.

**Файлы сессии:** изменены `smart_pump_reversal_bot.py`, `bot/tpsl_policy.py`, `tests/test_tpsl_policy.py`; новые `strategies/att1_v2_live.py`, `strategies/pair_stat_arb_v1.py`, `strategies/equities_swing_active_v1.py`, `backtest/robustness.py`, `scripts/archive_dead_strategies.py`, `scripts/pnl_by_sleeve.py` + тесты. Документ для Codex = ЭТОТ файл (`AUDIT_AND_FIXES_2026_06_08.md`), НЕ `docs/SESSION_LOG_2026_06_03.md`.

**После деплоя Codex:** (1) сверить P0 на живых сделках (`request_sl≈sl_price`), (2) проверить P0-b (`NO_ENTRY_HOURS_UTC` на сервере), (3) `pnl_by_sleeve.py` должен показать иную картину, (4) бэктест att1_v2 / pair_stat_arb / equities_swing через `robustness` (walk-forward+jitter+fee_sensitivity).

**Дальше по роадмапу:** веб (рефакторинг → красота + контроль сделок + P&L-модалка по рукавам + Кипр-налог с дисклеймером), funding-carry (≥10 циклов), Alpaca paper→real (PDT!), доводка TG/DeepSeek-слоя, постепенный разбор монолита. Полный список — раздел «РОАДМАП РАБОТ» и «ПЛАН РАЗВИТИЯ».

**Тон с владельцем:** честно, без обещаний доходности, с заботой (чувствителен к просадкам). Цель — устойчивая система, дающая воздух и выбор, + альтруизм (Кипр: семья, животные, природа). Он партнёр и судья; ИИ — ресёрчер/инженер; человек в контуре на всех решениях о деньгах/коде.
## 🌅 ВИДЕНИЕ ГОТОВОЙ СИСТЕМЫ (Opus, 2026-06-08)

Цель — не «разбогатеть быстро», а построить **устойчивую машину, которая живёт на дистанции, сама себя улучшает и не деградирует**. Дисциплинированный компаундер, дающий со временем воздух и выбор, а не гонку, чтобы не утонуть.

### Как она устроена, когда готова
1. **Несколько независимых рукавов** (чем меньше коррелируют — тем стабильнее общий результат):
   - Направленная крипта (ядро) — ATT1/breakdown/midterm/flat и их проверенные наследники.
   - Market-neutral (statarb pair trading, funding-carry) — зарабатывает и в падении.
   - Акции (Alpaca) — спокойный momentum/sector-rotation рукав.
   - Позже — новые источники, каждый входит только после доказанного OOS-эджа.
2. **Центральный «мозг» риска и аллокации**: режим-оркестратор + portfolio allocator распределяют капитал по рукавам, режут риск в плохих условиях, держат дневной −2% / общий −5% как нерушимые рейлы. Капитал владельца защищён в первую очередь.
3. **Петля самоулучшения, которая НЕ деградирует**:
   - непрерывный авторесёрч (sweeps) — но всегда через walk-forward + jitter (честная OOS-метрика, не подгонка);
   - champion-challenger: новые варианты живут в shadow/paper, в live попадают только при стабильном превосходстве на OOS;
   - авто-архив стратегий с PF<1 за окно — система сама убирает то, что перестало работать;
   - квартальный OOS-ретест всего на свежих данных.
4. **Полу-автоматика с человеком в контуре**: система предлагает (новые параметры, ребаланс, доливы), человек одобряет решения по деньгам. ИИ — неутомимый ресёрчер/инженер, владелец — хозяин и судья.
5. **Спокойная наблюдаемость**: веб + TG показывают equity, P&L по рукавам, запас до лимитов, «бот жив», налоговый план. Чтобы можно было смотреть без тревоги и жить, а не сидеть в терминале.

### Честная рамка ожиданий
Большинство «торговых систем» не бьют рынок; эдж нужно зарабатывать заново постоянно — поэтому петля ресёрча и анти-overfit дисциплина важнее любой одной стратегии. Win-условие — не миллиарды, а **надёжный процесс с низким стрессом, который выживает годами и даёт опции**: подушку, свободу выбора, меньше страха. Это достижимо инженерией и терпением, без магии и без обещаний.

### Роль ИИ по мере роста
Сейчас — аудит и сборка по запросу. С полной версией и API — постоянный партнёр: ведёт ресёрч-очередь, предлагает и проверяет идеи, следит за деградацией, готовит отчёты. Человек освобождается от рутины и принимает только ключевые решения.

---

## 📈 ПЛАН РАЗВИТИЯ — расширенный (стратегии, Alpaca, дисциплина)

### Как наращивать стратегии правильно (а не плодить файлы)
Урок этой сессии: 83 файла, реально работает горстка. «Много стратегий» даёт силу только через **конвейер**, а не через свалку. Конвейер для каждой новой идеи:
1. Идея → прототип (чистый модуль + unit-тесты на синтетике, как `pair_stat_arb_v1`).
2. Бэктест через `backtest/robustness.py` — **walk-forward + jitter + комиссии**. Принимаем только если медиана OOS PF>1 и нет катастрофичных фолдов.
3. Shadow/paper 2-4 недели (телеметрия без денег).
4. Champion-challenger: новый вариант против действующего; в live только при стабильном превосходстве на OOS.
5. Live с маленьким risk-mult → наращивать по факту.
6. Авто-архив при PF<1 за окно. Квартальный OOS-ретест всех.

### Целевой портфель стратегий (диверсификация по ТИПУ эджа)
Сила — в некоррелированных типах, а не в количестве однотипных:
- **Тренд/продолжение:** midterm, ATT1(v2), slope-break.
- **Mean-reversion:** flat/ARF1, range, support-bounce, (ML-фильтр позже).
- **Breakout/импульс:** IVB1, momentum-breakout, squeeze.
- **Пробой структуры (шорт):** breakdown, bear-continuation.
- **Market-neutral:** pair stat-arb (ETH/BTC, SOL/ETH), funding-carry.
- **Событийные/потоковые (позже):** liquidation-cascade, order-flow absorption (нужен сбор L2).
- По таймфреймам: свинг (4h/1d), интрадей (1h), скальп (5-15m) — раздельно валидировать.
Каждая ячейка матрицы «тип × таймфрейм × рынок» = слот; заполняем только проверенными.

### Alpaca (акции) — отдельный полноценный трек
- [ ] Подтвердить paper→real gate через `run_equities_walkforward_gate.sh` (он уже есть).
- [ ] Перепроверить intraday-рукав на **PDT-правило** (см. раздел Alpaca ниже) — критично для счёта < $25k.
- [ ] Диверсификация секторов (снижение корреляции внутри портфеля акций).
- [ ] Свериться, нет ли на стороне Alpaca аналога бага P0 (тайтовые стопы в `intraday_state.json`).
- [ ] Налоговый отчёт уже есть (`scripts/tax_monthly_report.py`) — переиспользовать для Кипр-калькулятора в вебе.

### Вспомогательные системы (доводка)
- [ ] Веб: красота + статус-полоса + контроль сделок (закрыть/долить) + P&L-модалка по рукавам (фундамент `scripts/pnl_by_sleeve.py` готов) + Кипр-налоги.
- [ ] TG-бот: дублировать ключевые действия и алерты; единый формат.
- [ ] DeepSeek-слой: использовать как генератор гипотез в очередь ресёрча (human-gate), НЕ как авто-исполнителя; всегда сверять с фактами (он не знал про P0).
- [ ] Самообучающая петля: regime_change → research_queue → sweep(walk-forward) → human-approve → live → degrade-monitor → auto-archive.

---

## 📊 Alpaca — разбор и можно ли сделать прибыльнее (2026-06-08)

Состояние: развит, торгует **paper** (в mirror открыты COST, ORCL). Два рукава: monthly momentum (hold ≤20д, native trailing 3.5%, top-14 вселенная, max 3 позиции) и intraday. Есть walk-forward gate, midmonth monitor, TG-отчёты, налоговый отчёт. Сделан добротно.

**Честно про «значительно прибыльнее»:**
- Это **спокойный/медленный рукав** — его задача стабильность и низкая корреляция с криптой, а не максимум прибыли. Разгонять его в «максимум» = менять его риск-характер. Лучшая ценность Alpaca — диверсификация, а не высокий return.
- Реалистичный эдж momentum на акциях — единицы–низкие двузначные % годовых. Это нормально для спокойного рукава.
- ⚠️ **PDT-правило (критично):** в США счёт < $25k ограничен 3 day-trade за 5 дней. Внутридневной рукав Alpaca на $500-1000 **упрётся в это регуляторно**. Нужно либо держать только monthly/swing (без day-trades) на малом счёте, либо понимать ограничение. Это, возможно, важнее любой настройки.
- Возможный аналог бага P0: в `runtime/live_mirror/intraday_state.json` у COST sl≈970.11 при entry 971.09 (~0.1%) и tp +0.15% — подозрительно тайтово для свинга. Codex: проверить обработку стопов в `equities_alpaca_*_bridge.py` (тот же класс ошибки?).

**Вывод:** Alpaca не «разгонять», а (1) снять PDT-риск, (2) проверить стопы, (3) пройти walk-forward gate → real маленькой суммой. Прибыльность ищем диверсификацией, а не агрессией.

---

## 🔒 Скан безопасности (2026-06-08) — базово чисто
- В ядре бота и вебе **нет** `eval/exec/shell=True/pickle.loads/os.system` (один `os.system(pip install)` только в research-скрипте — низкий риск, заменить на `subprocess` без shell).
- Web: CORS ограничен своим origin (не wildcard), отдельной debug-дыры не видно. Авторизация — TOTP.
- SQL: параметризовано; единственная f-строка — `PRAGMA table_info({table})` в служебном скрипте (имя таблицы внутреннее, не пользовательский ввод) — низкий риск.
- Ключи: `.env` 600, не в git; реальные только Bybit; cross-exchange read-only. ОК.
- **Рекомендации:** (1) заменить `os.system` на `subprocess.run([...])`; (2) при добавлении withdrawal-ключей — отдельный контур (whitelist+IP+ручное подтверждение, см. чек-лист); (3) ротация ключей через веб, чтобы убрать локальные копии `.env`.

---

## 🟢 Alpaca: активный свинг (PDT-safe) + бюджет + крипто-заметка (2026-06-08)

**Бюджет Alpaca: $500-1000 (потолок на обозримое время).** Дизайн строим под это, а не под крупный счёт.

### Почему не интрадей, а активный свинг
На счёте < $25k PDT-правило = максимум 3 day-trade за 5 дней. Поэтому «активный интрадей» на $500-1000 нежизнеспособен. Умный обход — **активный свинг**: холды 2-10 дней, шире вселенная, чаще ротация, чем monthly, но БЕЗ дневных round-trip'ов → PDT не мешает. Это «активнее и умнее» в рамках реальности малого счёта.

### Сделано (новый модуль, ждёт бэктеста Codex)
- **`strategies/equities_swing_active_v1.py`** + `tests/test_equities_swing_active.py` (6/6 зелёные): PDT-safe свинг-селектор. Скоринг = momentum (20d+60d, фильтр price>SMA50) + бонус за откуп на откате (RSI в зоне 40-60), отсев перекупленности (RSI≥78). `select()` ранжирует вселенную, `is_day_trade_safe()` гарантирует min-hold ≥2 дня. Чистый stdlib.
- Codex: прогнать через `backtest/robustness.py` (walk-forward+jitter+комиссии) на исторических акциях; если медиана OOS PF>1 — wire в `equities_alpaca_*_bridge.py` как «swing-active» режим (max_positions 3-4 под $500-1000), затем paper→real gate.

### Честно про прибыльность Alpaca
Это диверсифицирующий рукав; активный свинг повышает оборачиваемость, но не превращает в «золотую жилу». Реалистично — устойчивый умеренный вклад с низкой корреляцией к крипте. Главное на малом счёте: не нарушить PDT и не дать комиссиям/спреду съесть оборот.

### Крипто-стратегии (Elder, inplay, pump-fade, откаты) — честно
Написаны добротно; «улучшать» вслепую = overfit. Важнее: **Elder (`elder_triple_screen_v2`) тоже был жертвой P0** (не в белом списке → стоп затирался на 0.3%); inplay — в белом списке (ок); pump_fade — legacy pct (ок). Поэтому сначала P0-деплой → честная переоценка их эджа на live/бэктесте → потом точечные улучшения ТОЛЬКО через walk-forward. Хорошие стратегии нельзя оценивать, пока сломано исполнение.

### Файлы сессии (обновлено): +`strategies/equities_swing_active_v1.py`, +`tests/test_equities_swing_active.py`. Всего тестов сессии: 35/35 зелёные.

---

## ✅ Тест свинг-Alpaca на реальных данных (2026-06-08)
Прогон `equities_swing_active_v1.select()` на 14 реальных дневных рядах (data/equities_daily):
- Отсеял даунтренды (INTU, KO, PG, ADSK, SCHW — price<SMA50) — корректно.
- Ранжировал по моменту; поймал pullback-кейс (REGN: RSI 52, pullback 0.78) — то, ради чего задуман.
- Caveat: данные исторические (~2022-23), это smoke логики, не live-рекомендация. Полный walk-forward P&L — Codex.
Модуль готов: unit 6/6 + real-data smoke OK.

## 🎯 Цель владельца (зачем всё это)
Поднять рукава до десятков тысяч → недвижимость на Кипре для себя и семьи → часть дохода на помощь животным и природе Кипра. Не «разбогатеть», а превратить жизнь в жизнь. Строим под это честный, устойчивый фундамент — без обещаний, инженерией и терпением.


---

## 🔁 Реакция на ревью (2026-06-08, late) — что принято и внедрено

Внешний ревью (Claude/GPT/DeepSeek) — сильный, по делу. Внедрено сразу:
- **P0 надёжность:** `has_strategy_levels` теперь требует `tp>0 и sl>0` (а не только `is not None`) — нулевые/отрицательные уровни трактуются как «нет уровней» → pct-фолбэк. (smart_pump_reversal_bot.py ~6137)
- **Метрики сравнения стратегий:** в `backtest/robustness.py` добавлены `sortino_ratio` (штрафует только просадку), `geometric_mean_return` (не переоценивает волатильные), `fee_sensitivity` (прогон при 6/8/10 б.п. → вердикт fee_robust/fee_fragile для высокооборотных). +6 тестов. **Попутно пойман реальный баг — в robustness.py не было `import math` (уронил бы sortino в рантайме), исправлено.**
- Тестов в сессии теперь **41/41 зелёные** (8 tpsl + 18 robustness + 9 pair + 6 swing).

Принято в роадмап (не код сейчас):
- **P0-b:** возврат к `0,1,2 UTC` — временно; правильнее динамические окна (не входить за 30 мин до важных новостей / окна под рыночные сессии). Сделать настраиваемым.
- **Триаж 9 файлов:** перед `--apply` запустить бота в `dry_run` без них и убедиться в отсутствии ошибок импорта (страховка от динамического importlib по собранному имени).
- **Веб:** перед добавлением фич — отдельная задача Codex'у на рефакторинг `index.html` (3297 строк → вынести компоненты в модули), потом наращивать.
- **Кипр-налог:** крипта — 0% налога на прирост капитала ПРИ выполнении условий; но частый трейдинг налоговая может переквалифицировать в бизнес-доход (~12.5%). Калькулятор: «рекомендуется отложить X%» + явный дисклеймер «не юр. гарантия, свериться с кипрским налоговым консультантом».

---

## 🛡️ Слой целостности и «жучок-валидатор» (идея владельца, 2026-06-08)

Да, разумно и большей частью уже наполовину есть. Три уровня (строим, пока ядро дорабатывается):

### 1. Статическая проверка кода (CI)
- Тесты на каждое изменение (есть основа: 41 тест). Цель — гонять автоматически.
- Линтер `ruff` + типы `mypy/pyright` — ловят баги до рантайма (нашли бы missing `import math`).
- **Registry-consistency checker (дёшево, высокая ценность):** проверять, что каждая стратегия из allocator-политик/health-гейтов реально существует, и каждая wired-стратегия есть в реестре. Поймал бы запутанность «мёртвых» файлов. (есть `validate_sweep_configs.py`, `build_strategy_registry.py --drift` — расширить).

### 2. Runtime-watchdog (живые инварианты + алерты в TG)
Непрерывно проверяет и жалуется при нарушении:
- у каждой открытой позиции есть TP/SL на бирже и `request_sl≈sl_price` (ловит P0-класс);
- heartbeat свежий («бот жив»); регим не застрял; open-risk и дневной лимит в норме;
- live vs backtest в допуске (есть `live_vs_backtest_monitor.py` — это и есть watchdog деградации).

### 3. AI-советник в контуре (тот самый «ИИ на борту жалуется»)
Watchdog ловит аномалию → ИИ объясняет человеку простым языком в TG («breakdown PF<1 за 14д — рекомендую паузу») и проверяет предлагаемые изменения на вменяемость. Есть `deepseek_research_gate.py` — развить.
**Честный caveat:** ИИ бывает уверенно неправ (DeepSeek не знал про P0 → дал неверный вердикт). Поэтому ИИ — советник, НЕ финальный гейт. Реальные гейты — тесты + человек.

## 🚀 Технологии «на голову выше» (честно, без хайпа)
Не одна магия, а комбинация:
1. **Данные:** сбор и хранение тиков/L2-стакана (Parquet/DuckDB) — открывает order-flow стратегии, сейчас заблокированные отсутствием L2. Самый большой апгрейд.
2. **Реалистичный бэктест:** модель проскальзывания/частичных филлов/латентности — чтобы цифрам можно было верить.
3. **Анти-overfit по-взрослому:** walk-forward (есть) → purged combinatorial CV (López de Prado) — золотой стандарт.
4. **Регим через модели** (HMM/changepoint) вместо EMA-эвристики.
5. **ML как ФИЛЬТР качества сигналов** (gradient boosting), не как предсказатель цены и не RL — скромно и реалистично, после ядра.
6. **Observability:** структурные логи + метрики (Prometheus/Grafana) — видеть систему как настоящий деск.
7. ❌ Колокация/HFT-латентность — на вашем масштабе НЕ нужна, не тратить силы.

Приоритет: сначала ядро зарабатывает (P0 → доказанный эдж), параллельно строим слой целостности (#1-2 дёшевы), потом данные/бэктест-реализм. Это и отличает настоящую квант-систему от скрипта, который торгует.

---

## 🧠 Полнота знаний онбордового ИИ (идея владельца, 2026-06-08)

Требование: ИИ внутри бота должен знать **всё и всегда актуально** — архитектуру, все стратегии (live/off/архив), конфиги, последние изменения кода, live-состояние. Иначе он игрушка, а не инструмент. (Зачаток есть: `runtime/live_mirror/ai_context/full_context.json`, `AI_CONTEXT_BRIDGE_SPEC`.) Claude и GPT добавим в команду, когда пойдут деньги.

Как довести до «знает всё»:
- **Единый системный манифест** (авто-сборка): полный список стратегий со статусом, конфиги, реестр, последние git-изменения, live-инварианты → подаётся ИИ как ground-truth перед каждым решением.
- Манифест обновляется автоматически (cron / на каждое изменение), чтобы ИИ не отставал.
- Двойная польза: тот же манифест = **registry-consistency watchdog** (ловит рассинхрон стратегий и конфигов — ту самую запутанность «мёртвых» файлов).

### СДЕЛАНО до P0 (безопасно, без деплоя)
- `scripts/system_manifest.py` — сборка манифеста + проверка консистентности (orphan-ссылки в конфигах без файла; файлы-стратегии без ссылок нигде). Первый кирпич watchdog-слоя И ground-truth для ИИ.

### system_manifest.py — результат и как читать
Прогон 2026-06-08: 86 файлов стратегий, 20 wired-хендлеров, 22 ENABLE_*-флага, **14 «не подключённых»** = 9 подтверждённых архивных (совпадает с `archive_dead_strategies.py`) + 3 новых модуля этой сессии (att1_v2_live, pair_stat_arb_v1, equities_swing_active_v1 — ждут wiring) + gs1_live, pfs1_live.
Caveat (честно): «dead» = «имя не встречается как реальная ссылка». Новые модули будут в списке, пока их не подключат. Для удаления — только консервативный список `archive_dead_strategies.py` (учитывает ссылки в реестре/конфигах). Манифест = drift-детектор и ground-truth для ИИ, а НЕ авто-удалятор.
Для ИИ на борту: `python3 scripts/system_manifest.py --json runtime/system_manifest.json` (cron) → актуальный снимок всей системы.

---

## 🔬 Backtest Lab — гибкие сценарии + честное сравнение (СДЕЛАНО 2026-06-08)

`backtest/lab.py` + `tests/test_lab.py` (6/6). Слой ПОВЕРХ существующего движка (не замена):
- **Scenario** — задаёт что тестировать: список стратегий (вместе/отдельно), вспом-системы (regime/allocator/ai_gate) ON/OFF, символы, окно, fee/slippage, jitter, walk-forward.
- **report_from_result** — ЕДИНЫЙ честный отчёт на сценарий (одинаковый для всех → сравнимо): net, PF, win-rate, expectancy, avg win/loss, max DD, **Sortino**, **geo-mean**, **fee-drag % от gross**.
- **compare** — ранжирование сценариев по любой метрике.
- **ablation** — leave-one-out: вклад каждой стратегии/компонента (`FULL` минус `minus_X`); положительный = помогает, отрицательный = вредит (например, покажет, тянет ли стратегия портфель вниз).
- Исполнение — pluggable `runner(scenario)`: реальный = адаптер к `run_portfolio.py` (нужны данные/сеть → Codex/server); стаб — для офлайн-тестов фреймворка.

Применение (Codex): обернуть `run_portfolio.py` в runner → прогонять матрицу сценариев (каждая стратегия соло / ядро / ядро±компонент) через walk-forward+jitter → `compare`+`ablation` → честно видно, кто реально добавляет деньги, а кто шум. Это и есть «умная, гибкая, честная» бэктест-система.
