# -*- coding: utf-8 -*-
"""Настройки signal_copy. Ключ можно держать здесь или в переменной SIGCOPY_MT5_TOKEN."""
import os

MT5_URL   = os.getenv("SIGCOPY_MT5_URL", "http://127.0.0.1:22346/mcp")
MT5_TOKEN = os.getenv("SIGCOPY_MT5_TOKEN", "RYktr2yrD+qedCaZ6Oa2QDqFuHou4g83vXcRcKsXcH")

RISK_PCT       = 0.5     # риск на сделку, % от equity
MAX_RISK_PCT   = 2.0     # жёсткий потолок: выше — отказ
MAX_LOT        = 0.50
MAX_POSITIONS  = 3
DEFAULT_TP     = 4       # какую цель канала брать (1..4)


# Насколько далеко рынок может уйти от зоны входа, в долях риска.
# 0.5 = если цена ушла на половину расстояния до стопа, сигнал считается протухшим.
MAX_ENTRY_DRIFT_R = 0.5
MIN_RR            = 0.5   # минимальное отношение прибыли к риску по текущей цене

# Стоп должен быть осмысленно далеко, иначе объём взрывается, а позицию
# сносит рыночным шумом. Проверяем двумя способами.
MIN_STOP_SPREADS  = 8     # стоп минимум в N спредов от цены входа
MIN_RISK_KEEP     = 0.35  # текущий риск не меньше 35% от риска, заложенного в сигнал   # минимальное отношение прибыли к риску по текущей цене

# Разрешённые серверы. Демо определяем по имени сервера и полю type,
# а не по одному полю — на сломанном счёте оно врало.
ALLOWED_SERVERS = ("MetaQuotes-Demo",)
ALLOW_LIVE = False
