# Pair-arb — локальный санити-прогон (2026-06-30, Claude, песочница)

Цель: не ждать сервер — прогнать СУЩЕСТВУЮЩИЙ валидатор `scripts/validate_pair_arb.py`
(он уже переписан под realizable P&L: equal-notional, 4 fills fees, time-stop, funding)
на реальном кэше мажоров. Это лёгкий single-pair sim, не тяжёлый matrix (тот OOM'ит).

## Аудит ядра коинтеграции (`strategies/pair_stat_arb_v1.py`) — ЧИСТО ✓
Гейт `diagnostics()` дисциплинированный и без lookahead (срезы `[-lookback:]`, `[:i+1]`):
- beta>0 (OLS hedge ratio); OU half-life ≤ max_half_life (mean-revert достаточно быстро);
- |corr| возвращаемых ≥ 0.75; **проверка дрейфа беты ≤35%** (anti-spurious-cointegration).
Багов не нашёл. Docstring сам честно фиксирует: прошлый honest WF давал PF ~0.8, fee-fragile.

## Результаты (interval 60, lookback 168, fee 6bps, fee-sens 6/8/10)
| Пара | aligned bars | trades | net_pnl | WR | PF | verdict |
|---|---|---|---|---|---|---|
| ETH/BTC | 361 | 2 | −0.019 | 0% | 0.0 | fee_fragile |
| SOL/ETH | 361 | 5 | −0.032 | 20% | 0.34 | fee_fragile |
| SOL/BTC | 697 | 5 | +0.056 | 80% | 41.5 | fee_robust(*) |
| LINK/ETH | 361 | 1 | −0.005 | 0% | 0.0 | fee_fragile |

(*) PF 41 на 5 сделках = шум/оверфит-выброс (4/5 удачных по случайности). По нашей же
дисциплине такие PF-выбросы ОТБРАСЫВАЕМ, не выводим. Это НЕ эдж.

## Честный вывод
1. **Эджа не видно**: 3 из 4 пар в минусе; единственный «плюс» — 5-сделочный выброс.
   Полностью согласуется с docstring (PF ~0.8). Pair-arb пока НЕ доказан — как и carry.
2. **Данных локально критически мало**: 361–697 аплайнутых баров (~15–30 дней 1h) и
   1–5 сделок на пару при z=2.5. Этого НЕДОСТАТОЧНО для OOS-вердикта (нужно плато по
   ≥3/4 окон, а тут даже одного полноценного окна нет).
3. Снижать entry_z ради частоты = parameter-fishing → только в OOS-свипе, не вручную.

## Что нужно (для Codex/сервер, у него полный кэш)
- Прогнать `walkforward_pair_arb.py` на ДЛИННОЙ истории (мин. 6–12 мес 1h) по корзине
  ликвидных пар, отбор по OOS-плато, НЕ по PF-пику.
- Вернуть: trades/окно, net-of-fee PF по OOS-окнам, fee-sensitivity. Если медиана OOS
  PF ≤ 1 на достаточном N — pair-arb в архив гипотез, как и carry.

## Метод-вывод
Тулинг рабочий и честный — узкое место не код, а (а) объём данных и (б) тяжёлый WF,
который надо гнать на сервере. Локально подтвердил: эджа на доступном куске нет.
