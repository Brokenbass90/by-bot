# КАРТА ТЕХНОЛОГИЙ + ПЛАН РАЗВИТИЯ (2026-07-03, Claude). START HERE для стратегии.

Зачем всё написано и как этим правильно расти. Ничего не выбрасываем — берём выборочно.

## 1. КАРТА: зачем каждая технология (стадии одного конвейера)
НАЙТИ сетап (детекторы «что торговать»):
  range_filter, failed_breakout, structure_break(BOS/CHoCH), breakout_confirm, retest_quality,
  liquidity_sweep, pump_exhaustion, cascade_reversal(H4), elder_filter(конфлюэнс).
ОТОБРАТЬ инструменты/не жечь пустое (динамика):
  range_scanner (какие монеты во флете СЕЙЧАС), preflight_check (не гнать пустой gate),
  regime_hmm (в каком режиме рынок), news_session_filter (FX: не лезть в новости).
ИСПОЛНИТЬ хорошо:
  level_entry (лимит У уровня, не поздний close), smart_grid (сетка в крепком флете),
  trailing_stop (дать прибыли течь), slippage_model (честные издержки).
ВЗВЕСИТЬ РИСК:
  position_sizing (fixed-R), risk_manager (умный анти-мартингейл риск),
  exposure_gate (не задваивать коррелированные), sleeve_registry ((strategy x side) атомарно).
ДОКАЗАТЬ (не обмануть себя):
  wf_folds (purge+embargo OOS), oos_selector (отбор по плато, не по пику).
СЛЕДИТЬ + УЛУЧШАТЬ (ИИ-надзор):
  decision_bus (ИИ видит КАЖДОЕ решение+исход), edge_monitor (распад эджа онлайн),
  champion_challenger (промоут/демоут), research_orchestrator (еженедельный Proposal в TG на аппрув),
  run_checkpoint (прогоны переживают сон Mac).
=> Ни одна не лишняя: это find->select->execute->size->prove->monitor->improve.

## 2. ФАЗЫ РАЗВИТИЯ (как правильно расти)
Ф1 — ПЕРВЫЕ РУКАВА (сейчас): ATT1 short live (доказан, ждёт setup); ARF2 failed-breakout
  (в gate -> canary если OOS-символы держат); Alpaca $500 (владелец). Цель: 2-3 живых источника.
  Метод: ОДИН рукав за раз, минимум фильтров, за флагом, canary tiny.
Ф2 — ЧАСТОТА: range/пила блок (ASB2/ACB1/ARF2-split) на ДИНАМИЧЕСКОМ range_scanner ->
  «торговать каждый день». Пробои — через confirm+retest+regime (не сырой). Сетка — на мажорах/золоте.
Ф3 — СБОРКА ПОРТФЕЛЯ + ЖИВАЯ ПЕТЛЯ: вписать управляющий слой (decision_bus+edge_monitor+
  smart_risk+exposure+regime) вокруг рукавов. Портфель из нескольких некоррелированных.
  (Блупринт: LIVE_MANAGEMENT_LOOP_BLUEPRINT_2026_07_03.md.)
Ф4 — УМНОЕ УЛУЧШЕНИЕ ТЕХНОЛОГИЯМИ (то, о чём ты спрашиваешь): когда портфель собран и
  decision_bus накопил данные — улучшаем КАЖДЫЙ рукав А/Б-методом:
  * decision_bus атрибутирует: какой фильтр реально поднимает WR/expectancy;
  * добавляем технологию к рукаву ТОЛЬКО если A/B на OOS/live показал плюс;
  * research_orchestrator еженедельно предлагает изменения -> человек одобряет;
  * анти-мартингейл риск + regime-гейт держат просадку.
  Так технологии повышают качество торговли ПОСТЕПЕННО и измеримо, не «включить всё разом».

## 3. ИИ-АНАЛИЗ БОТА — да, встроено
- Непрерывно: edge_monitor (здоровье каждого рукава).
- Еженедельно: research_orchestrator (разбор стратегий/технологий -> Proposal в TG).
- По запросу: ИИ читает decision_bus и ищет аномалии/гипотезы -> в OOS.

## 4. ПРАВИЛА (не нарушать)
- Одно изменение за раз + A/B-замер. Технология заслуживает включение результатом.
- Отбор монет — автомат/динамика; аппрув ИИ — на уровне стратегии, не монеты.
- Смещение к простоте. 26 модулей = ящик, берём выборочно под рукав.
- OOS-first; риск лестницей через smart_risk; correlation+regime гейты обязательны для short-alt.
