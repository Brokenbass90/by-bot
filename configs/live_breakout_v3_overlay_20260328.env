# Narrow live overlay from the current live5 profile toward reconstructed_v3.
# Intentionally touches only breakout density/quality fields that improved the
# reproducible golden candidate without changing the rest of the stack.

BT_BREAKOUT_QUALITY_MIN_SCORE=0.53
BREAKOUT_QUALITY_MIN_SCORE=0.53
BREAKOUT_MAX_CHASE_PCT=0.13
BREAKOUT_MIN_PULLBACK_FROM_EXTREME_PCT=0.07
