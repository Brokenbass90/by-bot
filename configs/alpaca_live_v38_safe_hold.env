# Alpaca real-account preservation overlay.
#
# Keep the existing capped positions protected and managed, but do not let a
# refreshed candidate list rotate the account or submit replacement entries.
# Remove this overlay only after exact-live monthly parity passes review.

ALPACA_ALLOW_NEW_ENTRIES=0
ALPACA_CLOSE_STALE_POSITIONS=0
MONTHLY_MIDMONTH_ROTATION=0
