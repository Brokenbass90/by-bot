# Roadmap — 2026-05-17
**Обновлено:** 2026-05-17 после большой сессии Claude
**Период:** активный план на 30/60/90 дней

## Active Manager Roadmap — 2026-05-19

Этот блок теперь главный. Старые секции ниже оставлены как исторический контекст и постепенно будут ужиматься.

### Как ведём проект

- `ROADMAP_20260517.md` — один активный roadmap: что делаем дальше и почему.
- `docs/CODEX_CURRENT_STATE_20260518.md` — один handoff/current-state для нового чата Codex/Claude.
- `docs/JOURNAL.md` — длинный журнал фактически сделанных работ.
- Новые документы создаём только если это спецификация к коду. Всё остальное дописываем в эти 3 места.

### Главный вывод сейчас

- Alpaca monthly v38 hybrid — лучший доказанный кандидат: `WR 82.9%`, `+50.77%` за 24 месяца, PF `6.29`, но торговля редкая.
- Alpaca `v38-active` research — свежий 24m прогон 2026-05-19: `31` сделка, `WR 83.87%`, PF `8.2183`, compounded `+59.784%`, max monthly DD `-3.856%`. Кандидат в активную paper/shadow-ветку, не ломает обычную v38.
- Alpaca intraday v3 shadow — не годится для real money: `148` сделок за 360d, `WR 39.19%`, PF `0.8609`, net `-4.836%`.
- Crypto live не “сломано полностью”: сервер живой, allocator/router OK, но стратегии не конвертят setup scanner в сделки. Нужен blocker report, а не повышение риска.
- AI full-context cron включён: каждые 5 минут собирает scanner cards, heartbeat, allocator/router, trade events, research status и project doctor в read-only oracle pack.

### Alpaca: оставляем 2-3 версии

1. **v38 monthly hybrid top4** — оставить как основной compounder-кандидат для `$500` paper → real после проверки защиты и свежего paper-цикла.
2. **v38-active / more-active monthly** — оставить отдельной веткой: чаще, чем v38, но только paper/shadow до проверки текущего цикла и fills.
3. **Claude/new active Alpaca** — новая частая версия, но только через 2y backtest + segmented OOS + paper. Цель: выше частота, WR `>55%`, PF `>1.25`, positive expectancy, DD контролируемый.
4. **intraday v3 shadow** — оставить только как research baseline; не запускать real, пока PF < 1.

### Crypto: ближайший порядок

1. Сделать live blocker report: setup scanner card → strategy evaluated? → какой фильтр заблокировал вход.
2. Убрать диагностический шум `same_bar/cooldown`, чтобы видеть реальные причины.
3. Дождаться Korpaty/research queue:
   - `asb1_bull_chop_repair_v1` сейчас идёт, ранние результаты слабые;
   - далее `att1_density_v3_more_pivots_v1`;
   - далее `elder_v3_macro_off_full_relax_v1`.
4. В бой идут только стратегии с PF/expectancy/DD/trades, не “кажется красиво”.

### AI/operator

1. Включить cron для `scripts/build_ai_full_context.py`.
2. Подключить `full_context.json` к DeepSeek/web prompt как read-only oracle context.
3. Добавить proposal queue: AI может писать предложения, но не менять live params напрямую.
4. Увеличить memory TTL и сделать persistent memory summary.
5. Подключить trade forensics после каждой закрытой сделки.

### Когда бот реально начнёт торговать

- Alpaca paper уже активна; stale intraday state очищен, close orders ждут market open.
- Alpaca real `$500`: разумный путь — v38 monthly после проверки paper-protection/fills, не intraday v3.
- Crypto: не форсировать. Если после blocker report окажется, что foundation мешает входам, чиним в тот же день. Если фильтры честно не дают edge, ждём research winners.

### Ближайшие 5 рабочих задач

1. Проверить Alpaca fills после открытия рынка и состояние protective orders.
2. Найти/принять у Claude новую Alpaca active версию, прогнать 2y + segmented.
3. DONE 2026-05-19: включён AI full-context cron; verified `67` setup cards, regime, heartbeat, trade events, no-signal reasons.
4. DONE 2026-05-19: сделан crypto setup-to-entry blocker report; первый вывод: `33` cards blocked by zero-risk sleeves, `25` by same-bar/cooldown noise, `9` breakdown cards need missing reasons.
5. DONE 2026-05-19: добавлены grouped `breakdown_ns_*` counters и controlled restart при `open_trades=0`.
6. Следующее: per-symbol/evaluated counters для `flat`, потом решать ASB1/BRC1 shadow/paper.
7. После ASB1/ATT1/Elder результатов выбрать: promote to paper/shadow, доработать, или reject.

## Historical Snapshot — 2026-05-17

- **Bybit live:** auth OK, equity ~$123 USDT, 0 трейдов >3 недель (auth был сломан, теперь allocator блокирует)
- **Alpaca paper:** v38 hybrid держит UNH+GOOGL, ждёт активной v2 версии
- **Регим:** bull_chop, overlay есть (Claude добавил)
- **Главный блокер:** `portfolio_allocator` haircuts гасят все sleeves в 0 на малом депозите
- **Решение готово:** `ALLOCATOR_MODE=auto` спека ждёт Codex'а (вернётся через 2 дня)

## Historical Plan — 2026-05-17

### 🔴 ФАЗА 1: Allocator auto-tiering [PRIORITY ZERO]
**Спека:** `ALLOCATOR_DIAGNOSTIC_AND_BYPASS_20260517.md`
**Кто:** Codex (Claude написал спеку)
**Срок:** в день возвращения Codex'а + 24ч
**Acceptance:** ≥1 реальный trade за 24ч после restart, ≥3 трейда за 48ч
**Результат:** бот наконец торгует

### 🟡 ФАЗА 2: Alpaca dynamic_v1 (trailing + re-entry)
**Спека:** `ALPACA_DYNAMIC_V1_SPEC_20260517.md`
**Скрипт backtest'а:** `scripts/alpaca_dynamic_full_backtest.py` (ready)
**Кто:** Codex
**Срок:** неделя после Фазы 1
**Acceptance:** backtest 24 мес → delta ≥ +0.5pp/мес → paper deploy 14 дней → real money $500
**Ожидаемый результат:** 27% годовых v38 → 35-45% годовых dynamic_v1

### 🟡 ФАЗА 3: AI Context Bridge (oracle + advisor)
**Спека:** `AI_CONTEXT_BRIDGE_SPEC_20260517.md` (расширена до 3 стадий)
**Скрипт morning brief:** `scripts/ai_morning_brief.py` (ready, ждёт деплоя)
**Кто:** Codex
**Срок:** 2 недели после Фазы 2
**Acceptance:** DeepSeek шлёт ежеутреннее резюме в TG, TG-команды `/ai_*` работают, advisor suggestions проходят через approval flow

### 🟢 ФАЗА 4: Активация новых стратегий
**Файлы:**
- `strategies/alt_spike_rejection_v1.py` (ready)
- `strategies/alt_bear_regime_continuation_v1.py` (ready)
- `strategies/alt_whale_print_follow_v1.py` (ready)
- `strategies/alt_liquidity_sweep_reversal_v2.py` (ready)
- `strategies/alt_momentum_breakout_v1.py` (ready, новая)
**Backtest скрипт crypto:** `scripts/crypto_strategies_backtest.py` (ready)
**Кто:** Codex запускает autoresearch + добавляет в allocator policy
**Срок:** месяц после Фазы 1
**Acceptance:** каждая стратегия с PF > 1.3 в backtest → base_mult=0.5 → 7 дней live → mult=1.0
**Ожидаемый результат:** ~2-3x частота трейдов, дополнительные +5-15% годовых

## 30/60/90 дней

### Следующие 30 дней

**Цель:** Бот стабильно торгует, Alpaca v2 валидирован, AI работает в advisor mode

1. Codex запускает Фазу 1 → бот торгует
2. Запуск backtest для Alpaca dynamic_v1 + crypto strategies
3. Деплой ai_morning_brief в cron
4. Активация 5 новых стратегий
5. Mонторинг 30 дней: PnL, drawdown, win rate, allocator decisions
6. Через 30 дней: paper Alpaca стабилен → real $500

**Реалистичный месячный return на $123 + $500 paper:** 2-4% от $123 ($3-5) + paper не считаем
**Реалистичный месячный return на $1500 (если депозит):** 3-6% (~$45-90)

### 60 дней

**Цель:** $500 на Alpaca real, funding-carry активен, 8 sleeves работают

1. **Funding-carry executor** активирован — готовый код Codex'а на 413 строк, DRY_RUN → live
   - +10-15% годовых passive на свободный кэш
   - Не пересекается с trading risk
2. **Breakdown_v1 regime fix** — 5 строк env, вернёт стратегию на bear-фазах
3. **Champion-challenger framework v1** — каждая live стратегия имеет 2 shadow-варианта
4. **Web UI: AI suggestion panel** — карточка с pending suggestions от DeepSeek
5. **Cross-asset hedge logic** — снижаем leverage на крипте если BTC -5%/час

**Реалистичный месячный return:** 4-7% blended (с funding + dynamic Alpaca)

### 90 дней

**Цель:** $1000-1500 equity total, OANDA forex live (если получишь API), AI executor stage

1. **OANDA forex live** — bridge готов (`forex/oanda/{client,bridge}.py`), ждёт API ключа
2. **AI executor stage** — DeepSeek auto-executes 2 безопасных действия без approval (если 65%+ accuracy в advisor mode)
3. **Multi-timeframe confirmation** — entry на 5m только если 60m+240m выровнены
4. **Volatility-targeted sizing** — risk scaling по ATR вместо фиксированного 1%
5. **Sector rotation для Alpaca** — top-4 → top-2 + top-2 из второго сектора

**Реалистичный месячный return:** 5-10% blended

## Долгосрочно (3-6 месяцев) — спеки готовы

1. **Live retraining pipeline** — спека `LIVE_RETRAINING_PIPELINE_20260517.md` готова. Раз в неделю пере-тренируется на rolling 6mo, замыкает self-improvement loop
2. **DeepSeek weekly review** — спека `DEEPSEEK_WEEKLY_REVIEW_20260517.md` готова. ИИ предлагает 3-5 изменений каждое воскресенье
3. **Volatility-targeted sizing** — спека `VOLATILITY_SIZING_20260517.md` готова. Стабилизирует DD через ATR-нормализацию
4. **Sub-account separation** — спека `BYBIT_SUB_ACCOUNT_SEPARATION_20260517.md` готова. Изолирует funding-carry от main margin
5. **HF scalping layer** — концепт `HF_SCALPING_LAYER_CONCEPT_20260517.md`. Не делаем сейчас, активируем при $5k+ equity
6. **Genetic algorithm strategy evolution** — после стабильной работы champion-challenger 3+ месяца
7. **Phase 2 monolith refactoring** — 12k строк smart_pump → модули
8. **DeepSeek operator в auto-mode** — после 65% accuracy в advisor stage
9. **Цель через 12 мес:** $5k+ equity, 8-15% месячно, всё automated с human oversight только на критичные действия

## Зависимости и блокеры

| Что | Блокер | Решение |
|---|---|---|
| Bot торгует | Allocator haircuts | ALLOCATOR_MODE=auto (готово в спеке) |
| Alpaca trailing native | Fractional shares Alpaca rejects | Software trailing (dynamic_v1) — готов |
| OANDA forex live | Нет API ключа | Юзер регистрируется, получает demo→live |
| AI auto-execute | Нет audit history | Сначала 30 дней advisor mode |
| Phase 2 рефакторинг | Risk поломать live | Сначала 60 дней стабильной работы |

## Что НЕ в roadmap (намеренно)

- ❌ 10-30%/мес сразу — нереально на $1500 без длинной валидации
- ❌ Высокочастотный scalping — отдельная архитектура, не вяжется с текущим bot'ом
- ❌ Market-making — требует low-latency infra
- ❌ Options trading — Bybit perpetuals only, Alpaca stocks only

## Команды для контроля прогресса

```bash
# На сервере:
cat /root/by-bot/runtime/orchestrator_state.json  # текущий режим
python3 /root/by-bot/scripts/allocator_diagnostic.py  # после Фазы 1
cat /root/by-bot/runtime/alpaca_backtest_report_*.json  # после Фазы 2 backtest
cat /root/by-bot/runtime/crypto_backtest_report_*.json  # после crypto backtest
tail -50 /root/by-bot/logs/ai_morning.log  # после Фазы 3 cron
```

---

## Сессия Claude 2026-06-10 — что сделано и бэклог (для Active Manager)

**Сделано (всё в `AUDIT_AND_FIXES_2026_06_08.md` части 1-10 + `STRATEGY_PACKAGE_2026_06_10.md`):** 2 бага движков починены; честная матрица ~15 стратегий; боевой пакет ~62-65%/год (risk 1%); Elder починен (strict+majors, 3/4 окна в плюсе); ATT1 v2 валидна (плюс в обеих фазах); модули confidence_risk + dd_throttle (DD 12.7→7.6%) + edge_canary — все с тестами, default OFF→shadow; план к 120% (5 рычагов); видение+ИИ-этапы.

**Бэклог Claude (не успел — беру следующими сессиями):**
1. B6 Stack Comparison Gate — скрипт «стратегия BARE vs WITH-обвязка» (ARF1-класс ловушек; у меня уже есть методика).
2. Cron-скрипт edge_canary (чтение live_trade_events → runtime/edge_canary.json) + TG-формат алерта.
3. Profit-skimming модуль + /skim команда (спека в VISION).
4. Champion-challenger конкретно: ATT1 v1 vs v2 протокол сравнения в paper.
5. breakout_v2 (если серверная матрица не спасёт пробои вверх) + bear-trend шорт.
6. CEO-письмо генератор (еженедельный отчёт из pnl_by_sleeve).

**Для Codex (порядок):** деплой-лист в аудите часть 10 → серверная матрица полной вселенной (+grid_smart_v1) → O(n²) обвязка → maker-входы → extra_router/blocker-панель/TG-кнопки.
